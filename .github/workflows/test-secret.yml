name: Secret Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Dummy secret usage
        run: |
          echo "AWS_SECRET_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE" >> secret.txt
          cat secret.txt
